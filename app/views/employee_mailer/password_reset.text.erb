Per resettare la tua password clicca il seguente link:

<%= edit_password_reset_url(@employee.reset_token, email: @employee.email) %>

Questo link scadrà tra due ore.

Se non hai richiesto di resettare la tua password, ignora questa email e la tua password non cambierà.
